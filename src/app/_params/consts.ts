import { PlayingState } from "@/app/_states/state";

export const stateNotPlaying = [PlayingState.title, PlayingState.finishing];
export const stateIsPlaying = [PlayingState.playing, PlayingState.pausing];
